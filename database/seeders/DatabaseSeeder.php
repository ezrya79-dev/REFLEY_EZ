<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * En développement, le jeu de démonstration s'invoque explicitement :
     * `php artisan db:seed --class=DemoSeeder`. Ce seeder par défaut reste
     * vide — en production, le premier compte se crée via `refley:admin`.
     */
    public function run(): void
    {
        //
    }
}
